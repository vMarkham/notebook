more notes

Git Notes

Unstaged
  Will not be committed in the next commit
Staged
  Will be committed
Commit
  Save the staged files in the repository

  Git init
    Create a new git repo
  git status
    check status of staging area
  git add
    add files to the staging area
  git commit
    commit files to the git repo
  git log
    Check the commit history
5 pieces of a commit
  commit ID
  Signature (Name + email)
  Timestamp
  Message
  Changes
